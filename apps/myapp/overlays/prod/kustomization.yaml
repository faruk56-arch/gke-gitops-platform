apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: myapp

helmGlobals:
  chartHome: ../../chart

helmCharts:
  - name: myapp
    releaseName: myapp
    namespace: myapp
    valuesInline:
      image:
        tag: "prod"